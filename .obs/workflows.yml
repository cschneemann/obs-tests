workflow:
  steps:
    - branch_package:
        source_project: Examples
        source_package: demo1
        target_project: Examples:Dev
submit_tag_to_product:
  #  steps:
  #    - branch_package:
  #        source_project: Examples
  #        source_package: demo1
  #        target_project: Workflows:Examples
    - submit_request:
        source_project: Examples:Dev
        source_package: demo1
        target_project: Eco:10
          #target_package: demo1
          #description: 'New version has been tagged'
  filters:
    event: tag_push
